// TODO: Finish news page

import 'package:flutter/material.dart';
import 'Data/airvibe_methods.dart';
import 'home.dart' show AQIRipper;

class BingNewsPaper extends StatefulWidget {
  const BingNewsPaper({super.key});

  @override
  State<BingNewsPaper> createState() => _BingNewsPaperState();
}

class _BingNewsPaperState extends State<BingNewsPaper> {
  @override
  Widget build(BuildContext context) {
    return Container(
      
    );
  }
}

class NewsScreen extends StatelessWidget {
  const NewsScreen({super.key});
  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('News Screen. Coming Soon...', style: TextStyle(color: Colors.white)),
    );
  }
}